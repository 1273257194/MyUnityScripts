1.UI层级

    UIView 和 UIPanel所在层级不同
    UIView 在长显层和弹窗层
    UIPanel 在默认层级
    层级设置在初始化中设置ViewLayer 
    层级关系 弹窗<-长显<-默认层级
    
热更工程内包含 
1、游戏逻辑代码
2、UI逻辑代码
3、SDK调用
4、插件封装调用

Unity工程
1、热更启动代码
2、插件代码
3、SDK封装代码